# Supabase

Postgres gerenciado + pgvector + storage + RLS.

## Setup inicial
1. Habilitar extensao vector.
2. Rodar migrations em `migrations/` (ordem numerica).
3. Configurar RLS por tenant (medico so ve a propria base).

As migrations sao geradas nas proximas etapas do backlog (itens 2, 3, 10).
