{
  "name": "careplus-predict-v2",
  "version": "0.1.0",
  "private": true,
  "description": "CarePlus Predict V2 — monorepo (mobile + web + backend)",
  "workspaces": [
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "echo \"configure por workspace\" && exit 0",
    "typecheck": "echo \"configure por workspace\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
